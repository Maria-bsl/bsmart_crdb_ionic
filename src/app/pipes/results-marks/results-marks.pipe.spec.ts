import { StudentsMarksPipe } from './results-marks.pipe';

describe('StudentsMarksPipe', () => {
  it('create an instance', () => {
    const pipe = new StudentsMarksPipe();
    expect(pipe).toBeTruthy();
  });
});

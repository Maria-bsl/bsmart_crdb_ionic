import { SubscriptionsPipe } from './subscriptions.pipe';

describe('SubscriptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new SubscriptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
